# 변경 내역

## 0.8.0

**단계 그림을 설치 파일에 넣지 않습니다**

- 그림은 게임 파일에서 추출한 것이고 **Blizzard Entertainment 의 저작물**입니다.
  196개를 설치 파일에 담아 배포하는 것은 그 저작물을 복제해 배포하는 것입니다.
  나눠 쓰기 곤란한 구조였습니다.
- 이제 제어창의 **오버레이 → 그림 내려받기** 를 눌러야 받아옵니다. 받는 곳은
  `tools/fetch-icons.js` 가 늘 쓰던 [sc2-planner](https://github.com/BurnySc2/sc2-planner)
  저장소이고, 저장 위치는 userData 입니다. 각 PC 가 자기 사본을 가져가는 것이라
  배포하는 파일에는 블리자드 자산이 들어가지 않습니다.
- 설치본에 남는 것은 `manifest.json` 하나입니다 — 한글 용어와 파일 이름을 잇는
  표이고, 이 프로젝트가 만든 자료입니다. 설치 파일이 2.2MB 가벼워집니다.
- **묻지 않고 받지 않습니다.** 아이콘 기본값이 `없음` 이라 안 쓰면 아무것도
  내려받지 않고, 버튼을 누를 때만 네트워크를 씁니다. "정지 상태에서는 SC2 를
  읽지 않는다" 와 같은 원칙입니다.
- 받는 동안 진행 상황(`48 / 196`)이 제어창에 표시됩니다. 196개를 느린 회선으로
  받으면 멈춘 것처럼 보이기 때문입니다.
- 실패한 파일은 건너뛰고 계속합니다. 196개 중 195개는 구멍 하나 있는 정상
  아이콘 세트이지, 아무것도 없는 것보다 훨씬 낫습니다. 다시 누르면 빠진 것만
  받습니다.
- 받는 도중 끊겨도 반쪽 파일이 남지 않습니다. 임시 이름으로 받아 다 받은 뒤에
  옮깁니다.

**라이선스와 고지**

- `LICENSE` 파일을 추가했습니다. `package.json` 에 MIT 라고만 적혀 있고 정작
  파일이 없었습니다. 코드는 MIT 이고, 게임 자산은 그 범위가 아니라는 것을
  분리해서 적었습니다.
- README 에 **비공식 도구이며 Blizzard Entertainment 와 무관**하다는 고지와,
  글꼴·게임 데이터 읽기 방식에 대한 설명을 넣었습니다.
- 편집기의 가져오기 문구에서 특정 사이트 이름을 뺐습니다 — `JSON 열기…`,
  `가져오기 — JSON 익스포트 / 텍스트`.

## 0.7.3

**패널 드롭 섀도를 걷어냈습니다**

- 0.7.0 에 넣은 드롭 섀도가 잘못된 판단이었습니다. 오버레이 창은 내용 크기에
  정확히 맞춰지므로(`fitOverlay` 가 패널 높이를 그대로 창 높이로 씁니다) 패널이
  창을 꽉 채웁니다. 경계 상자 **바깥**에 그려지는 그림자는 갈 곳이 없습니다.
- 그래서 창 안에 남은 그림자는 **둥근 호와 직각 모서리 사이 부채꼴 조각뿐**
  이었고, 네 꼭지점에 어두운 직각 귀퉁이로 나타났습니다.
- 그림자가 펼쳐질 여백을 창에 만들어 봤지만, 그러면 **그림자가 게임 화면 위로
  번집니다.** 패널이 얹혀 있는 표면이 아니라 게임 자체에 떨어지는 것이라, 흰
  화면에서는 오버레이 둘레에 회색 얼룩으로 보입니다. 되돌렸습니다.
- 그림자를 완전히 제거했습니다. 패널은 자기 배경에서 끝나고, 바깥으로 나가는
  것은 아무것도 없습니다. 남은 `box-shadow` 는 전부 패널 안쪽입니다 — 유리층
  바닥선, 현재 단계 레일 글로우, 아이콘 테두리.
- 밝은 지형에서의 경계는 이제 테두리 헤어라인이 혼자 맡습니다.

**미리보기가 실제 창 조건을 재현하지 못했습니다**

- 비교 페이지의 iframe 이 패널보다 커서 그림자가 펼쳐질 자리가 있었습니다.
  실제 창에는 그 자리가 없는데, 미리보기에서는 멀쩡해 보였습니다. 창 크기에
  얽힌 것은 미리보기만으로 판단하면 안 됩니다.
- 미리보기 배경 스텁이 `body` 에 패딩을 걸고 있어서 여백 관련 측정을 덮어쓰고
  있었습니다. 제거했습니다.

## 0.7.0

**오버레이를 다시 그렸습니다**

- 글꼴을 **Pretendard** 로 바꿨습니다 (SIL OFL 1.1, `src/renderer/fonts/OFL.txt`).
  링크가 아니라 번들입니다 — 오버레이는 네트워크가 없는 `file://` 페이지이고,
  게임 위에 뜨는 글자가 늦게 도착하면 안 됩니다. 가변 폰트 하나로 45~920 전
  구간을 덮어서, 패널이 쓰는 굵기 위계(본문 400 / 시계 550 / 인구수 600)를
  파일 네 개 없이 유지합니다.
- **패널 배경이 지도 색을 빨아들이고 있었습니다.** 뒷배경에 채도를 +10% 걸고
  있어서 사막 맵에서는 누렇고 얼음 맵에서는 파랬습니다. 채도를 −30%, 밝기를
  −18% 로 뒤집었습니다. 유리 뒤를 중성 회색으로 눌러야 패널 색이 어느 맵에서든
  같습니다.
- **그림자가 없어서 밝은 지형에서 가장자리가 뭉갰습니다.** 접지·중간·앰비언트
  3단 램프를 넣었습니다. 큰 블러 하나는 후광으로 보이고, 단계를 나눠야 깊이로
  읽힙니다.
- 테두리를 **마스크로 오려낸 그라데이션 헤어라인**으로 바꿨습니다. `border` 는
  단색 하나만 받는데, 사방이 균일하게 빛나는 물체는 없습니다. 위·왼쪽에서 밝고
  중간에서 사라졌다가 아래에서 반사광으로 조금 돌아옵니다.
- 헤더·푸터의 1px 실선은 표 테두리처럼 보여서, **양끝이 페이드되는 선**으로
  바꿨습니다. 패널 안에 상자를 하나 더 그리지 않고 구획만 나눕니다.
- 현재 단계를 채운 블록에서 **왼쪽 액센트 레일**로 바꿨습니다. 색 사각형이 눈과
  글자 사이에 끼지 않습니다. 레일 두께는 왼쪽 여백에서 도로 빼야 합니다 —
  안 그러면 그 줄만 시간·인구수 칸이 3px 밀립니다.
- **임박 표시가 행 전체를 깜빡여서 글자가 같이 떨렸습니다.** 이제 레일만
  점멸합니다.
- 완료된 단계의 취소선을 뺐습니다. 흐리게 처리와 하는 말이 같은데 읽기만
  나빠집니다. 지난 단계도 지금 어디쯤인지 세는 데는 쓰입니다.
- **인구수가 본문 글자와 거의 같은 밝기라 단계 이름과 경쟁했습니다.** 한 단계
  낮춰서 행동이 먼저, 숫자가 나중에 읽히게 했습니다. 정렬도 오른쪽에서 가운데로
  옮겼습니다 — 자릿수가 바뀌면 좌우로 4.5px 흔들리지만, 인구수는 대부분 두
  자리라 실사용에서 드러나지 않습니다.
- 아이콘과 글자 사이가 행 기본 간격 6px 뿐이었습니다. **10.5px** 로 벌리고,
  그만큼 창도 넓혔습니다(아이콘 크게 기준 424 → 428px). 여백만 주면 단계 글자
  칸을 빼앗아 긴 문장이 더 일찍 잘립니다.
- 슬롯 번호 배지가 빌드 이름에 비해 작아서 각주처럼 보였습니다. 제목 대비
  0.72 → 0.82 비율로 키우고 배지 상자도 같이 넓혔습니다.

**기본 효과음이 소리 파일로 바뀌었습니다**

- 지금까지 기본음은 코드로 만든 사인파였습니다. 이제 번들된 `assets/cue.wav`
  가 기본입니다. 불러온 파일과 **같은 경로로 흘려보냅니다** — 재생 경로를 두 개
  관리하지 않으려고요.
- 합성음은 지웠지 않고 **대체재로 남겼습니다.** 번들 파일을 못 읽으면 오버레이가
  조용해지는 게 아니라 예전 소리로 울립니다.
- 원본 뒤쪽 671ms 가 무음이었습니다. 감쇠 꼬리 100ms 만 남기고 잘라
  1.40초 241KB → **0.83초 143KB** 가 됐습니다. 들리는 소리는 같습니다.

## 0.6.0

**용어 사전을 나무위키 SC2 문서와 전부 대조했습니다 (수정 32 · 추가 19)**

- 사전의 **6개 중 1개꼴로 이름이 틀려 있었습니다.** 하필 그럴듯해 보이는 것들이라
  게임을 아는 사람이 봐도 넘어갈 만했습니다: `첨탑`(→ 둥지탑), `일꾼로봇`(→ 지게로봇),
  `수송관 연결망`(→ 땅굴망), `야마토 포`(→ 무기 재장비).
- 대조는 **영문명이 병기된 SC2 문단**을 직접 읽어서 했습니다. `Evolve Flyer Carapace`
  ↔ `비행체 갑피` 처럼 1:1 로 맞춘 것만 반영했고, 추측으로는 아무것도 바꾸지
  않았습니다. 검색 요약은 SC1 섹션을 물어오는 일이 있어 신뢰할 수 없었습니다.
- 업그레이드 계열: `함선 무기` → 우주선 무기, `원거리 공격` → 발사 공격,
  `지상 방어력` → 지상 갑피, `고성능 자동 조준 장치` → 정밀 보안 자동추적기.
  `건물 장갑` 은 공허의 유산 4.7.1 에서 통합되어 사라진 이름이라 **신소재 강철 장갑**
  으로 바꿨습니다.
- 종족마다 단어가 갈리는 건 정상입니다. 영문부터 테란·프로토스는 `Weapons/Armor`,
  저그는 `Attacks/Carapace` 로 다릅니다. 저그만 `방어력` 을 쓰던 게 문제였습니다.
- 유닛별 연구 14개: `지옥불 예열기` → 지옥불 조기점화기, `착암 나노 강화` → 천공 발톱,
  `스마트 서보` → 지능형 제어 장치, `은폐` → 은폐장, `초음속 비행` → 초비행 회전날개,
  `신경 재생` → 신경 재구성, `굴착 발톱` → 땅굴 발톱, `근육 강화` → 근육 보강,
  `홈 파인 등뼈` → 가시 홈, `공기막 등껍질` → 기낭 갑피, `공명 검광` → 공명 파열포,
  `중력 가속기` → 중력 가속, `중력 구동기` → 중력 구동, `열 광선 연장` → 열 광선 사거리.
- **빠져 있던 19개를 채웠습니다.** 차량 및 우주선 장갑, 공중 장갑, 키틴질 장갑,
  합성 동화 작용, 적응형 발톱, 진동 가시뼈, 신경 기생충, 자기장 가속기, 방해 매트릭스,
  카두세우스 반응로, 첨단 탄도 시스템, 그림자 걸음, 음이온파 수정, 유동성 추진기,
  구조 불안정장치. 이제 정규 업그레이드 88개 중 사전에 없는 것은 캠페인 전용
  `Frenzy` 하나뿐입니다.
- **아이콘 누락이 0 이 되었습니다.** 파일명 규칙이 어긋난 것들을 손으로 이었습니다:
  `advanceballistics`(원본 오타), `Tectonic_Destabilizers`(다른 명명 규칙),
  그림자 걸음은 `점멸` 이 아니라 암흑 기사 전용 아트(`stealth-blink`)가 따로 있었습니다.
  진동 가시뼈만 팩에 협동전 케리건 버전밖에 없어 그것을 씁니다 — 같은 이름·같은
  업그레이드라 `궤도 사령부` 때와 달리 오해할 여지가 없습니다.
- 옛 표기는 별칭으로 남기지 않았습니다. 남겨두면 틀린 이름이 조용히 계속
  동작해서, 다시 뽑은 빌드에 섞여도 눈에 띄지 않습니다.

**단계 글자 크기와 가로 폭을 따로 조절합니다** (제어창 · 오버레이)

- 지금까지는 `크기` 하나에 헤더·목록·푸터가 전부 묶여 있었습니다. 정작 경기 중에
  읽는 건 빌드 목록뿐인데, 그것만 키우면 창까지 같이 커졌습니다.
- `단계 글자` 는 목록만 키웁니다. 창 크기는 그대로라 긴 단계가 `…` 로 잘리고,
  그때 `가로 폭` 으로 넓히면 됩니다. 두 축을 나눈 이유가 이것입니다.
- 시간·인구수 칸도 함께 커집니다. 고정해두면 글자만 커져서 숫자가 칸을 넘칩니다.
- 칸 폭은 각 줄의 글자 크기가 아니라 설정값에 직접 비례합니다. 현재 단계 줄은
  글자가 더 큰데, 줄마다 칸이 달라지면 **목록이 세로로 안 맞습니다.**
- 그림은 이 설정을 따라가지 않습니다. 그림은 글자가 아니고, 그림 칸 너비는 창
  너비 계산에 들어가는 값이라 여기서 키우면 둘이 어긋납니다.
- `가로 폭` 은 결과 픽셀을 함께 보여줍니다 (`125% · 475px`). `크기`, `아이콘`
  설정이 같은 계산에 들어가서 배율만으로는 결과를 알 수 없습니다.
- 남은 시간 글자를 0.74rem → 0.82rem 으로 키웠습니다. 단계가 임박한 순간 보는
  숫자가 이것 하나인데 눈에 띄게 작았습니다.

**효과음을 직접 고를 수 있습니다** (제어창 · 효과음)

- `불러오기…` 로 파일을 고르고, `기본음으로` 되돌립니다. 기본값은 지금까지의
  내장 합성음 그대로입니다.
- **파일은 복사해서 보관합니다.** 경로만 기억하면, 받은 폴더를 정리한 순간 경기
  중에 소리만 조용히 사라지고 화면에는 아무 표시도 안 납니다.
- 소리는 파일이 바뀔 때 한 번만 디코딩해서 오버레이가 들고 있습니다. 단계마다
  파일을 읽지 않습니다.
- **재생 실패를 삼키지 않습니다.** 확장자는 힌트일 뿐이고 실제 재생 가능 여부는
  디코딩해야 압니다. 그런데 디코더는 렌더러에만 있어서, 오버레이가 결과를 되돌려
  보내고 제어창이 표시합니다. 안 그러면 재생되는 소리와 실패한 소리가 둘 다
  '조용함' 으로 똑같습니다.
- 형식이 안 맞거나 5MB 를 넘으면 메시지만 뜨고 **쓰던 소리는 그대로 유지됩니다.**
- 볼륨은 내장음과 다르게 걸립니다. 내장음은 생사인파라 눌러야 하지만, 고른 파일은
  이미 원하는 레벨이라 설정값을 그대로 씁니다.
- WAV · MP3 · OGG · OPUS · M4A · AAC · FLAC · WEBM, 5MB 까지.

**모의 시계가 실제보다 40% 빨랐습니다**

- `npm run dev` 의 모의 시계가 실시간의 1.4배로 돌았습니다. 군단의 심장까지는
  인게임 시계가 그렇게 갔지만, **공허의 유산부터 인게임 시계는 실시간입니다.**
  요즘 빌드 오더 타이밍도 전부 실시간 기준입니다.
- 오버레이는 모의 배지를 일부러 숨기기 때문에, dev 로 띄워놓고 실제 게임과
  비교하면 원인을 짚기 어려웠습니다. 제어창 시계에는 `(mock)` 이 붙습니다.

## 0.5.0

**익스포트에 다른 종족 빌드가 섞여 들어오는 문제**

- 베스핀 익스포트는 **빌드 하나가 아닙니다.** `tree` 가 내 종족으로 키가 잡히고,
  사이트가 그 세션에서 열어본 종족 트리를 전부 담아 보냅니다. 그래서 PvZ
  익스포트 안에 직전에 보던 저그 빌드 트리가 그대로 들어있습니다.
- 분기 목록에 그게 다 나와서 두 파일이 섞인 것처럼 보였습니다. 이제
  `race` / `source.matchup` 이 지목하는 트리만 보여줍니다.
- **더 심한 문제도 같이 고쳤습니다.** 키 순서상 찌꺼기 트리가 앞에 와서, 기본
  선택이 **1단계짜리 테란 껍데기**였습니다. 프로토스 PvZ 빌드를 열면 `사령부`
  한 줄만 들어왔고, 그대로 저장하면 쓰레기 파일이 됩니다.
- 분기 `id` 는 종족끼리 겹칩니다 (트리마다 `main` 이 있습니다). 그래서 `id` 로
  찾기 **전에** 종족으로 먼저 걸러냅니다 — 안 그러면 다른 종족 빌드가 잡힙니다.
- 제외한 트리는 보고합니다: `이 익스포트에 다른 종족 트리도 들어있어
  제외했습니다 (Terran 1개, Zerg 3개)`. 헤더에도 어느 트리를 골랐는지
  `Protoss 트리 8개 분기` 로 표시됩니다.

**승률 0% 표기**

- 정찰 반응 분기는 승률 없이 `wr: 0` 으로 옵니다. `608판 승률 0%` 로 보여서
  표본이 가장 많은 분기가 무조건 지는 빌드처럼 읽혔습니다. 인구수 `0` 과 같은
  관례로 보고 `승률 —` 로 표시합니다.

**용어 사전 (3종족 175개)**

- **종족 접두사가 붙은 키를 인식합니다.** 베스핀 익스포트가 업그레이드를 게임
  내부 이름 그대로 `ZergMissileAttacks1` 로 보내는데, 사전에는
  `MissileAttacks1` 로만 있어서 영어로 남았습니다. 이제 접두사를 떼고 다시
  찾습니다 — 접미사가 이미 종족마다 다르므로(테란 보병 무기, 프로토스 지상 무기,
  저그 근접/원거리 공격) 다른 종족 업그레이드로 잘못 갈 수 없습니다.
  `Zergling` 처럼 사전에 있는 키는 애초에 이 경로를 타지 않습니다.
- **업그레이드 2·3단계를 채웠습니다.** 1단계만 있어서 2/2 를 올리는 빌드는
  그대로 또 걸렸습니다. 3종족 무기·장갑·방어력·보호막 전 계열입니다.
- 아이콘 도구의 **잘못된 누락 보고를 고쳤습니다.** 아이콘이 있는데도 없다고
  21개를 보고했습니다 (파일명 규칙으로 못 찾았지만 손으로 매핑된 것들). 지금은
  실제로 없는 `궤도 사령부` 하나만 보고합니다.

## 0.4.0

**유닛·건물 그림 (아이콘)**

- 오버레이 설정에 `아이콘 — 없음 / 작게 / 크게` 를 넣었습니다. 기본값은 없음
  입니다. 게임을 잘 모르는 사람이 글자만 보고는 따라가기 어렵다는 문제 때문에
  넣었고, 그 사람 PC 에서만 `크게` 로 켜두면 됩니다 (설정은 PC 별로 저장됩니다).
- `작게` 는 줄 높이를 그대로 두고 글자 왼쪽에 작은 그림만 붙입니다. `크게` 는
  그림을 키우고 줄도 넓힙니다. 글자는 두 모드 모두 그대로 남습니다 — 그림이
  '무엇을', 글자가 '몇 개를' 알려줍니다.
- 아이콘을 켜면 **창이 그만큼 넓어집니다.** 아이콘 열을 글자 자리에서 빼앗으면
  긴 단계가 잘리기 때문입니다.
- 단계 문장에서 한글 용어를 알아서 찾습니다. 긴 것이 이깁니다 — `병영 기술실`
  이 `병영` 보다, `원거리 공격 2단계` 가 `원거리 공격` 보다 먼저 잡힙니다.
  손으로 쓴 빌드 349단계로 확인해서 348단계에 그림이 붙었고, 안 붙은 하나는
  `상대 네 번째 기지 압박` — 그림이 없는 게 맞는 문장입니다.
- 못 알아본 단계는 그냥 글자만 나옵니다. 아이콘 폴더 자체를 못 읽으면 제어창이
  `아이콘 파일을 찾지 못했습니다` 로 알려줍니다 — 옵션이 고장 난 것처럼 보이지
  않게요.
- 그림 출처: [sc2-planner](https://github.com/BurnySc2/sc2-planner) (MIT) 가 게임
  파일에서 추출해둔 래더용 버튼 아이콘 183개. **그림 자체는 Blizzard
  Entertainment 저작물**이라 개인·비상업 용도로만 씁니다. 받아오는 도구는
  `node tools/fetch-icons.js` 이고, 한 번 받으면 다시 실행할 필요는 없습니다.
- `궤도 사령부` 만 원본 팩에 아이콘이 없습니다. 비슷한 `사령부` 그림으로
  대체하지 않았습니다 — 초보자가 사령부를 새로 짓는 것으로 오해할 수 있어서,
  글자만 두는 쪽이 안전합니다.

**게임이 시작되면 자동으로 시작** (설정, 기본 꺼짐)

- 켜두면 정지 상태에서 1초마다 게임 시작을 확인하고, 게임에 들어가는 순간
  `Ctrl+Alt+S` 를 누른 것과 같이 시작합니다. 로딩 화면에서 핫키를 누를 타이밍을
  맞출 필요가 없습니다.
- 제어창이 그동안 **`대기 중`** 으로 표시됩니다. 정지됨과 구분됩니다.
- 한 판 끝나고 정지하면 다시 대기로 돌아가므로, 연속으로 여러 판 할 때 매 판
  시작을 신경쓰지 않아도 됩니다.
- 실행 중에는 감시하지 않고, 옵션을 끄면 즉시 감시도 멈춥니다. 정지 상태에서
  아무것도 읽지 않는다는 원칙의 유일한 예외이고, 이 옵션을 켠 동안만입니다.
- 게임 안인지 판별하는 조건을 한 곳(`isInGame`)으로 합쳤습니다. 감시와 실행 중
  폴링이 서로 다른 기준으로 판단할 여지를 없앴습니다.

## 0.3.0

**매치업 자동 선택이 이제 실제로 동작합니다**

- 그동안 빌드를 한 번이라도 고르면 그 고정이 **영구히** 남아 자동 선택을
  덮어썼습니다. `config.lastBuild` 로 저장되어 앱을 다시 켜도 유지됐으므로,
  사실상 자동 선택이 꺼진 상태였습니다.
- 이제 **자동 선택 켜짐** 상태에서 슬롯 핫키·목록 클릭은 **그 게임만** 덮어쓰고,
  게임이 끝나면 자동 선택으로 돌아옵니다. 저장되지도 않습니다.
- **자동 선택 꺼짐** 상태에서는 고른 빌드가 계속 유지됩니다. 기본값은 꺼짐입니다.
- 고정된 빌드는 목록에 `고정 — 눌러서 해제` 로 표시되고, 다시 누르면 풀립니다.

**★ 즐겨찾기**

- 같은 매치업 빌드가 여러 개일 때 자동 선택이 어느 것을 고를지 정합니다.
  이전에는 파일명 순서로 첫 번째만 잡혔습니다.
- 정확 일치가 항상 범용(`vs: *`)을 앞서고, 같은 계층 안에서만 즐겨찾기가
  순서를 이깁니다.

**엉뚱한 빌드를 띄우지 않습니다**

- 매치업에 맞는 빌드가 하나도 없으면 목록의 첫 빌드로 조용히 떨어지던 것을
  고쳤습니다. 저그로 플레이하는데 TvZ 빌드를 띄우는 일이 있었습니다. 이제
  오버레이에 `ZvT 빌드가 없습니다` 로 표시됩니다.
- **내 플레이어를 못 찾는 경우**도 같습니다. 사람 상대 1v1 에서 이름이 비었거나
  틀리면 `내 플레이어를 못 찾았습니다 / 제어창 설정에 내 플레이어 이름을
  넣으세요` 로 표시됩니다.
- 게임 전에는 여전히 목록의 첫 빌드를 보여줍니다 — 그때는 읽을 매치업이 없고,
  빈 오버레이를 띄울 이유도 없습니다.

**슬롯**

- 빌드 목록이 **슬롯 번호 순서**로 정렬됩니다. 이전에는 파일명 순서라서
  `slot: 1` 인 빌드가 목록 세 번째에 있을 수 있었고, 목록과 핫키가 어긋났습니다.
- **슬롯 번호 중복 설정을 막았습니다.** 편집기의 슬롯 드롭다운이 이미 쓰이는
  번호를 `3 — 올인` 처럼 그 빌드 이름과 함께 보여주고, 고르면 두 빌드의 번호가
  **서로 바뀝니다.** 이전에는 중복 선언이 그냥 저장되고 "먼저 온 쪽이 이김" 으로
  조용히 처리되어, 어느 빌드가 그 번호를 갖는지 예측할 수 없었습니다.
- 편집 중인 빌드가 고른 번호에도 그 이름이 붙습니다. 저장 후 목록이 어떻게
  보일지 드롭다운에서 그대로 읽힙니다. 단 이미 쓰이는 번호는 그 빌드 이름을
  유지합니다 — 누구와 교환될지가 저장 전에 봐야 하는 정보입니다.
- 상대 파일은 `slot:` 줄만 고쳐지므로 `#` 주석과 나머지 내용이 보존됩니다.
- 슬롯이 9개뿐이라 핫키를 못 받는 빌드도 **매치업 자동 선택과 목록 클릭으로는
  그대로 쓸 수 있습니다** (빌드 개수 제한은 없습니다).

**제어창 정리**

- 설명 문구를 대폭 줄였습니다. 핫키 목록은 접어뒀습니다 (`핫키` 클릭).
- 상태 표시도 짧게: `읽지 않음 · 가짜`, `— 인식됨`, `— 자동 구분됨`.
- **내 플레이어 이름** 칸 아래에 SC2 가 알려준 실제 플레이어 이름이 표시되고,
  일치하면 초록색으로 바뀝니다. 배틀태그인지 인게임 이름인지 추측할 필요가
  없습니다.

## 0.2.0

**Vespene.gg 익스포트 가져오기**

- 사이트에서 내보낸 JSON 을 한글 빌드오더로 변환합니다. 사이트를 긁지 않고
  직접 내보낸 파일만 읽습니다.
- 편집기(`Ctrl+Alt+E`) → `Vespene.gg JSON 열기…` — 익스포트에 들어있는 분기를
  **승률·표본과 함께** 목록으로 보여주고 골라서 가져옵니다. 메인 분기가 항상
  승률이 가장 높지는 않습니다.
- 영→한 용어 사전 3종족 153개. 표기는 공식 한국어 명칭입니다. 사전에 없는
  용어는 영어로 남고 **반드시 보고됩니다**.
- 변환 옵션: 사이트 메모(영어) 포함 여부, 상황부 단계 포함 여부, 반복 생산
  압축/제외. 바꾸면 즉시 다시 변환됩니다.
- 명령줄로도 됩니다: `node tools/import-build.js <파일.json> --list`

**변환이 정리해주는 것**

- 사이트는 해병 한 기마다 한 줄씩 넣습니다(빌드당 100줄 가까이). `해병 계속
  생산` 한 줄로 합쳐 106단계를 53단계로 줄입니다.
- `TechLab` 만으로는 무슨 기술실인지 알 수 없어, 붙는 건물을 찾아 `병영 기술실`
  / `우주공항 기술실` 로 이름을 완성합니다.
- 사령부가 1:45 에 궤도 사령부가 되는 것을 별도 단계로 냅니다.
- `opening` / `early` / `mid` 를 `[오프닝]` `[초반]` `[중반]` 구간으로 붙입니다.
- 프로가 31% 만 하는 단계는 `// 상황부 31%` 로 표시하고, 빈도가 너무 낮으면
  제외합니다.

**앱 중복 실행 방지**

- 두 번째로 실행하면 즉시 종료되고 실행 중인 앱의 제어창이 열립니다. 이전에는
  두 번째 인스턴스가 전역 핫키를 등록하지 못한 채로 떠 있어서, 모든 단축키가
  조용히 안 먹는 상태가 됐습니다.

**배포판 구성**

- 배포판에는 `seed/` 만 들어갑니다. 작업용 `builds/` 는 포함되지 않습니다.
  이전에는 개발 폴더에 있던 빌드가 전부 딸려 나갔습니다.
- 지금은 샘플 한 개만 심습니다. **기존 사용자의 빌드는 건드리지 않습니다** —
  `%APPDATA%/sc2-build-overlay/builds/` 에 파일이 있으면 시딩을 건너뜁니다.

---

## 0.1.0

첫 배포. 오버레이, 제어창, 빌드오더 편집기, 단계 전환 알림음, 오버레이 위치
프리셋, 내용에 맞춰지는 창 크기, 앱 아이콘, Windows 설치 파일·포터블 실행 파일.

> 이 번호로 여러 번 다시 빌드했기 때문에, `0.1.0` 이라고 적힌 실행 파일들이
> 서로 내용이 다릅니다.

---

버전은 **외부에 내보낼 때만** 올립니다. 개발 중 반복 빌드는 같은 번호로 다시
말면 됩니다.
